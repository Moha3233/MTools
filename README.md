# MTools
# Forward Toolkit — Streamlit Workbook App

## Files
- `forward_toolkit_app.py` — Streamlit app (main)
- `auth.py` — simple SQLite auth helpers
- `requirements.txt`

## Quick local run
1. Create venv and install:
```bash
python -m venv venv
source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
